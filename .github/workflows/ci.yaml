name: Build Pipeline 

on:
  pull_request:
    types:
      - opened 
      - edited
    branches:
      - dev
      - main
      - qa
  push:
    branches:
      - dev

jobs:
  build:
    
    runs-on: ubuntu-latest
    steps: 
      - name: Checkout
        uses: actions/checkout@v3
        run: ls -l

      - name: image build 
        run: echo "building the commit ${GITHUB_SHA::8}"
